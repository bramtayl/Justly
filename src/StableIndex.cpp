#include "StableIndex.h"

StableIndex::StableIndex(int chord_index_input, int note_index_input,
                         int column_index_input)
    : chord_index(chord_index_input),
      note_index(note_index_input),
      column_index(column_index_input) {}