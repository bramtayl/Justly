xml = coverage.xml
filter = library/
exclude-throw-branches = yes

# In Debug builds Q_UNREACHABLE() lowers to an always-skipped assert call,
# which would otherwise show up as an uncovered line
exclude-lines-by-pattern = .*Q_UNREACHABLE.*
